<%= render :partial => "/shared/menu",
           :locals => {
             :dom_id => 'menu',
             :css => 'menu'
           } -%>
<h1 id='logo'><%= link_to RefinerySetting.get(:site_name), root_url(:only_path => true) %></h1>
<h2 id='tagline'>Ruby on Rails <abbr title="Content Management System">CMS</abbr> that supports Rails 3</h2>
<%= stylesheet_link_tag('blog') if is_blog? %>